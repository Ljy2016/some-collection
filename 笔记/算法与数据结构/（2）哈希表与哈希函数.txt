	
	哈希表：
	
	哈希表是一种数据结构，它把关键字映射为表中的位置，通过关键字来获取元素。
	
   （哈希表是一种将关键字映射为表中的位置来获取元素的数据结构。）
   （以该数据结构存放元素的数组也叫作哈希表）
   
    哈希函数：
	
	根据关键字生成地址的函数。
	
	哈希碰撞：
	
	对于不同的关键字哈希函数生成了相同的哈希地址。
	
	
	哈希表与数组的区别：
	
	数组是编程语言提供的一种数据类型
	哈希表是一种数据结构
	两者都以数组为基本存储形式，但是两个不同的概念。